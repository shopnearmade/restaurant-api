# MongoDB connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/restaurantDB

# JWT secret key (use a long, random string in production)
JWT_SECRET=your_super_secret_key_here
